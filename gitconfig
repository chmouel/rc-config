[include]
    path = ~/.gitconfig.local

[gitreview]
	username = chmouel

[github]
	user = chmouel
	oauth-token = 6bf3f1dd643c4a208ffcb29dfac503bd664c7ac0

[hub]
	protocol = https

[user]
	name = Chmouel Boudjnah
	email = chmouel@chmouel.com

[alias]
    cc = !"find $(git rev-parse --show-toplevel) -name '*.pyc' -delete"
    jenkins-error = git-jenkins-error
    co = checkout
    log = log --no-merges
	st = status -sb
	amend = commit --amend
    dlast = diff --patch-with-stat --stat HEAD^1
    dlog = show --summary
    review = git-review
    sp = stash show -p
    show-merged = git-show-merged
    vdiff = difftool -y --tool=vimdiff2
    vlast = difftool -y --tool=vimdiff2 HEAD^1
    elast = difftool -y --tool=ediff HEAD^1
    ediff = difftool -y --tool=ediff
    pullall = !git pull && git submodule update --init --recursive
    bnm = branch --no-merged
    ls = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --no-merges
    ll = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --numstat
    browse-gh = git-browse-gh
    gh-clone = !bash -c '[[ $1 == */* ]] || git clone http://github.com/openstack/$1 && git clone http://github.com/$1' -
    browse-gerrit = git-browse-review
	g = grep --break --heading --line-number
    cp = cherry-pick
    branch-date = "!git for-each-ref --sort=committerdate --format='%1B[32m%(committerdate:iso8601) %1B[34m%(committerdate:relative) %1B[0;m%(refname:short)' refs/heads/"
    branch-cleanup = git-branch-cleanup
    bd = branch --delete

[core]
    quotepath = false
    whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
    excludesfile = ~/.gitexclude
	pager = less -FRSX
	autocrlf = input

[diff]
	mnemonicprefix = true

[merge]
	tool = emerge

[push]
	default = matching

[color]
  ui = true

[grep]
  lineNumbeR = true
[filter "media"]
	clean = git media clean %f
	smudge = git media smudge %f
	required = true
