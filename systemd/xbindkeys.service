[Unit]
Description=XBindKeys key grabber
After=environment.target

[Service]
ExecStart=/usr/bin/xbindkeys -n -v
KillMode=process
ExecCondition=bash -c '[[ -n ${DISPLAY:=""} && -z ${WAYLAND_DISPLAY:-""} ]] && exit 0 || exit -1'
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
